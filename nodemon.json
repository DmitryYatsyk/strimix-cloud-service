{
  "watch": ["src/**/*"],
  "ext": "ts,js,json,yaml",
  "ignore": ["node_modules/**/*", "build/**/*", "dist/**/*", "*.test.ts", "*.spec.ts"],
  "exec": "npx tsc --noEmit && tsx src/index.ts",
  "env": {
    "NODE_ENV": "development"
  },
  "delay": 1000,
  "verbose": true
}
